package com.good.base;

import com.good.base.BaseService;

/**
 * BaseServiceImpl
 *
 * @Description: uuuuuuuuuuuuuuuuu
 * @Author: tretert
 * @Time:1
 */

public class BaseServiceImpl<T>  implements BaseService<T> {

    /*@Autowired
    private BaseMapper<T> baseMapper;

    public int save(String tableName,T t) {
        return baseMapper.save(tableName,t);
    }

    public int delete(String tableName,int id) {
        return baseMapper.delete(tableName,id);
    }

    public int update(String tableName,T t) {
        return baseMapper.update(tableName,t);
    }

    public List<T> getAll(String tableName) {
        return baseMapper.getAll(tableName);
    }

    public T getById(String tableName,int id) {
        return baseMapper.getById(tableName,id);
    }*/
}
